name: Build the project code
on:
  workflow_call:
    inputs:
      cache_paths:
        type: string
        required: false
        default: ""
      elixir-version:
        type: string
        required: false
        default: "1.14.3" # Define the elixir version
      otp-version:
        type: string
        required: false
        default: "25.1" # Define the OTP version
      mix-env:
        type: string
        required: false
        default: test
    secrets:
      HEX_ORGANIZATION_COINGAMING_KEY:
        required: true

defaults:
  run:
    shell: bash

env:
  MIX_ENV: ${{ inputs.mix-env }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04

    steps:
      - name: Set up Elixir
        id: beam
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ inputs.elixir-version }}
          otp-version: ${{ inputs.otp-version }}

      - name: Install ExEnv
        run: mix archive.install hex ex_env 0.3.2 --force

      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          submodules: recursive
          token: ${{ secrets.GH_TOKEN }}

      - name: Install protoc
        run: sudo apt-get install protobuf-compiler

      - name: Restore Cache
        uses: actions/cache@v2
        id: cache
        with:
          key: v2-release-${{ hashFiles('mix.lock') }}-${{ github.sha }}

          restore-keys: |
            v2-release-${{ hashFiles('mix.lock') }}-${{ github.sha }}
          path: |
            ${{ inputs.cache_paths }}
            ~/.mix
            ./deps
            ./_build

      - name: Add coingaming organization
        if: steps.cache.outputs.cache-hit != 'true'
        run: mix hex.organization auth coingaming --key ${{secrets.HEX_ORGANIZATION_COINGAMING_KEY}}

      - name: Add oban repo
        if: steps.cache.outputs.cache-hit != 'true' && ${{secrets.OBAN_FINGERPRINT}}
        run: mix hex.repo add oban https://getoban.pro/repo --fetch-public-key ${{secrets.OBAN_FINGERPRINT} --auth-key ${{secrets.OBAN_LICENSE_KEY}}

      - name: Get deps and protobuf escript
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          mix deps.get
          mix escript.install hex protobuf 0.10.0 --force
          echo "$HOME/.mix/escripts" >> $GITHUB_PATH

      - name: Generate test proto files
        if: steps.cache.outputs.cache-hit != 'true'
        run: scripts/gen-test-proto.sh

      - name: Compile dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: mix deps.compile

      - name: Compile
        if: steps.cache.outputs.cache-hit != 'true'
        run: mix compile
